# Web — surface specification

The web surface presents records to end users.

- **FR-001**: The surface MUST display a record by its stable id.

It consumes the core write path (FR-001) indirectly through the API.
