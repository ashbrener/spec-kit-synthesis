# ADR-001: Cursor pagination

**Status**: Accepted

## Decision

The API uses opaque cursor pagination. This is a repo-local decision (API-ADR-001); a bare ADR-001
here must NOT cross-match the core repo's ADR-001.
